
export const metadata = {
    title: "Jobs - drugboard.ai",
    description: "drugboard.ai - Display Science and Scientific Connect",
};

export default function RootLayout({ children }) {
    return (
        <html lang="en">
        <body>{children}</body>
        </html>
    );
}
